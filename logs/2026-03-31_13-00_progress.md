# Progress Log

## Timestamp

2026-03-31 13:00:08 PDT

## Current Progress

- Added per-opcode latency accounting to the stats surface.
- Added focused regressions for scalar and DMA latency-stat reporting.
- Revalidated the full repository suite after the engine/stats refinement.

## Current Implementation Status

- `perf_modeling/stats.py` now records latency counters such as:
  - `latency.addi.samples`
  - `latency.addi.total_cycles`
  - `latency.addi.max_cycles`
  - `latency.dma_copy.samples`
  - `latency.dma_copy.total_cycles`
  - `latency.dma_copy.max_cycles`
- `perf_modeling/engine.py` now records one latency sample for each issued instruction using the planned completion cycle.
- `tests/test_latency_stats.py` validates both scalar and DMA latency accounting.
- Full verification is green with `uv run pytest tests -q` at 54 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, the deleted `main.py`, the untracked `docs/` tree, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether latency reporting should later expose averages explicitly or leave that to post-processing from samples and totals.
- Whether DMA source and destination reservations should still be decomposed into more detailed service windows in a later timing pass.

## Next-Step Plan

- Commit the per-opcode latency-accounting slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next simulator/stats refinement after checkpointing this update.
