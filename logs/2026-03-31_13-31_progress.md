# Progress Log

## Timestamp

2026-03-31 13:31:00 PDT

## Current Progress

- Added field-level filtering for curated CLI reports.
- Exposed one global `--report-match` control so multi-row reports can be narrowed by case-insensitive substring matching.
- Revalidated the CLI-specific path and the full repository suite after the reporting refinement.

## Current Implementation Status

- `scripts/run_sim.py` now accepts `--report-match`.
- Multi-row report modes such as `latency`, `occupancy`, `memory`, `contention`, `stalls`, `units`, and `isa` now honor that filter.
- `tests/test_run_sim_cli.py` now validates both the subprocess parser path and a direct helper path for report filtering.
- Full verification is green with `uv run pytest tests -q` at 66 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, the deleted `main.py`, the untracked `docs/` tree, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether later CLI/reporting work should add machine-readable CSV export in addition to JSON and stdout reports.
- Whether the next thread should pivot from reporting refinements back into deeper simulator timing/modeling behavior.

## Next-Step Plan

- Commit the report-match CLI slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next simulator/reporting refinement after checkpointing this update.
