# Progress Log

## Timestamp

2026-03-31 14:18:42 PDT

## Current Progress

- Added CLI support for preloading DRAM images before simulation begins.
- Extended the CLI regression suite to validate DRAM preloads with a real assembly program and a DRAM dump round-trip.
- Revalidated the full repository suite after changing simulator state initialization behavior.

## Current Implementation Status

- `scripts/run_sim.py` now supports repeatable `--dram-load ADDRESS:PATH` arguments.
- The CLI applies each DRAM preload after program load and before execution, so external data can seed workloads without being linked into the program image.
- `tests/test_run_sim_cli.py` now synthesizes a temporary DRAM image plus a small assembly program, preloads the image, round-trips the loaded word through a dependent store, and verifies the dumped bytes.
- Full verification is green with `uv run python -m compileall perf_modeling tests scripts toolchains` and `uv run pytest tests -q` at 107 passing tests.

## Current Roadblocks

- `git push origin main` is still expected to fail because DNS/network resolution to `github.com` remains blocked in this environment.
- The worktree still contains unrelated user-side changes: deleted `main.py`, deleted `tests/isa/README.md`, and untracked `scripts/codex.py`.

## Current Questions

- No open technical questions are currently tracked.

## Pending Design Decisions

- Whether the CLI should expose symmetric scratchpad preload support in addition to DRAM preload support.
- Whether the next slice should continue on workload-input ergonomics or pivot back into simulator timing/model fidelity.

## Next-Step Plan

- Commit the DRAM preload slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next simulator milestone after checkpointing this update.
