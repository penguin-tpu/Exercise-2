# Progress Log

## Current Progress

- Started and completed the new `INSTRUCTIONS.md` item for C workload coverage and direct C-to-ELF execution.
- Added freestanding C counterparts for the scalar workload examples under `tests/workload/`.
- Extended the CLI source path so `scripts/run_sim.py` accepts `.c` inputs directly and compiles them through the existing GNU RV32 toolchain wrapper.

## Current Implementation Status

- `perf_modeling/cli_support.py` now treats `.c` as a compileable source suffix alongside assembly sources.
- `toolchains/riscv32/assemble_to_elf.py` is now documented as a generic freestanding source-to-ELF path and uses `-O2`, `-fno-pic`, and `-msmall-data-limit=0` so C examples compile into simulator-friendly bare-metal RV32I code without stack-heavy debug output or small-data `gp` assumptions.
- Added four C workload examples:
  - `tests/workload/scalar_int_matmul.c`
  - `tests/workload/scalar_vector_add.c`
  - `tests/workload/scalar_reduce_sum.c`
  - `tests/workload/scalar_relu.c`
- Extended regression coverage in:
  - `tests/test_toolchain_wrapper.py`
  - `tests/test_workload_examples.py`
  - `tests/test_run_sim_cli.py`
- Updated `README.md` and `toolchains/riscv32/README.md` to describe direct C source support.

## Current Roadblocks

- `git push origin main` remains blocked by the environment DNS restriction for `github.com`.

## Current Questions

- None at the moment.

## Pending Design Decisions

- The newly added Perfetto visualization item in `INSTRUCTIONS.md` is still pending and is the next clear instruction-driven milestone.
- The log-compaction item in `INSTRUCTIONS.md` is also still pending after that.

## Next-Step Plan

- Commit this C source milestone and attempt the required push.
- Proceed to the next unchecked instruction item: add Perfetto-compatible visualization/export support, using the referenced external logging logic as the shape reference.
