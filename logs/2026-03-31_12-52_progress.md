# Progress Log

## Timestamp

2026-03-31 12:52:31 PDT

## Current Progress

- Added aggregate contention counters so memory-system stalls are visible without manually summing per-resource keys.
- Extended the contention regressions to validate the new DRAM and scratchpad aggregate counters.
- Revalidated the full repository suite after the stats-path change.

## Current Implementation Status

- `perf_modeling/engine.py` now derives and records:
  - `dram.contention_stalls`
  - `memory.contention_stalls`
  - `scratchpad.bank_conflict_stalls`
  - `scratchpad.port_conflict_stalls`
  - `scratchpad.contention_stalls`
- `tests/test_memory_contention.py` now checks those aggregate counters in:
  - DRAM contention cases
  - scratchpad bank-conflict cases
  - scratchpad port-conflict cases
  - no-conflict setup/overlap cases
- Full verification is green with `uv run pytest tests -q` at 51 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, the deleted `main.py`, the untracked `docs/` tree, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether contention reporting should eventually track per-bank or per-port histograms, not just aggregate stall totals.
- Whether DMA source and destination reservations should still be decomposed into more detailed service windows in a later timing pass.

## Next-Step Plan

- Commit the aggregate contention-stats slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next simulator/reporting refinement after checkpointing this update.
