# Progress Log

## Timestamp

2026-03-31 13:17:00 PDT

## Current Progress

- Added an occupancy-summary view to the simulator CLI on top of the existing queue-depth histogram output.
- Derived average queue depth and peak depth per unit directly from the sampled occupancy counters.
- Revalidated the CLI-specific path and the full repository suite after the new report behavior.

## Current Implementation Status

- `scripts/run_sim.py` now prints `report occupancy_summary ...` lines before the per-depth occupancy buckets.
- The occupancy summary includes total samples, average depth, and peak depth per unit.
- `tests/test_run_sim_cli.py` now validates both the builtin subprocess output and a synthetic histogram helper path for the occupancy summary.
- Full verification is green with `uv run pytest tests -q` at 59 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, the deleted `main.py`, the untracked `docs/` tree, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether the CLI should next expose stall-focused summaries so issue bottlenecks are easier to inspect without raw stat filtering.
- Whether unit reports should later add utilization percentages derived from total cycles and busy cycles.

## Next-Step Plan

- Commit the occupancy-summary CLI slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next reporting refinement after checkpointing this update.
