# Progress Log

## Current Progress

- Converted `perf_modeling.config` from a single module into a package so it can own reusable preset configurations.
- Added named example hardware presets and wired `scripts/run_sim.py` to select them with `--config`.
- Extended CLI regression coverage to validate preset exposure and preset selection through the real entrypoint.

## Current Implementation Status

- `perf_modeling/config/__init__.py` now owns the configuration dataclasses that previously lived in `perf_modeling/config.py`.
- `perf_modeling/config/presets.py` now provides packaged example configurations:
  - `baseline`
  - `tiny_debug`
  - `balanced_ml`
  - `throughput_ml`
- `scripts/run_sim.py` now accepts `--config` and uses the named preset to build the simulator.
- The stats JSON export and manifest now record the selected config name.
- `tests/test_run_sim_cli.py` now verifies both the preset registry and end-to-end CLI selection of `tiny_debug`.
- `INSTRUCTIONS.md` now has the config-subpackage item marked `-[x]`.

## Current Roadblocks

- Remote push remains blocked by environment DNS resolution failure for `github.com`.

## Current Questions

- None.

## Pending Design Decisions

- Whether the next unchecked slice should add representative example workloads or start the written architecture specification.
- Whether future config presets should become workload-oriented families such as memory-bound versus tensor-heavy, or remain simple hardware-shape examples.

## Next-Step Plan

- Continue with the remaining unchecked instruction items, likely the representative workloads or the specification documents.
- Keep the preset registry stable so future docs and examples can refer to config names without churn.
