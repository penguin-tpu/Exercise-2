# Progress Log

## Current Progress

- Added fetch-stall cycle accounting so control instructions now surface frontend blocking directly in simulator stats.
- Extended shared CLI summaries and pipeline reports to include fetch-stall cycles and fetch-stall percentage.
- Added focused RV32I and CLI regressions to pin the new visibility behavior.

## Current Implementation Status

- `perf_modeling/engine.py` now increments `fetch_stall_cycles` during cycle sampling whenever the frontend is stalled behind an in-flight control instruction.
- `scripts/run_sim.py` now carries `fetch_stall_cycles` and `fetch_stall_pct` through the compact run summary, the `summary` report, the `pipeline` report, and the JSON manifest summary.
- `tests/test_rv32i.py` includes a dedicated control-flow timing test proving fetch-stall cycles accumulate across control-instruction latency.
- `tests/test_run_sim_cli.py` verifies the updated pipeline/summary text and manifest schema.

## Current Roadblocks

- Remote push remains blocked by environment DNS resolution failure for `github.com`.

## Current Questions

- None.

## Pending Design Decisions

- Whether to break fetch stalls down further by cause, such as branch/jump/trap/system-return, instead of one aggregate counter.
- Whether to expose fetch-stall detail as a dedicated CLI report in addition to the pipeline and summary views.

## Next-Step Plan

- Continue from this visibility slice into deeper frontend timing fidelity, likely by separating fetch-stall causes or surfacing additional control-path timing counters.
- Keep the CLI summary and manifest aligned with any new pipeline-level stats so automation and human-readable reports stay consistent.
