# Progress Log

## Current Progress

- Generalized grouped manifests beyond sweep-only usage.
- One experiment manifest can now drive either a single run or a sweep, while the older `--sweep-manifest-json` path remains supported as a compatibility alias.

## Current Implementation Status

- Reworked [perf_modeling/cli_support.py](/home/tk/Desktop/Architect/perf_modeling/cli_support.py) to expose `ExperimentManifest` plus `load_experiment_manifest()`, while retaining `load_sweep_experiment_manifest()` as a wrapper for compatibility.
- Extended [scripts/run_sim.py](/home/tk/Desktop/Architect/scripts/run_sim.py) with:
  - `--experiment-json`
  - manifest resolution for single-run `config` as well as sweep settings
  - mutual-exclusion validation between `--experiment-json` and `--sweep-manifest-json`
  - manifest-aware single-run config selection and JSON artifact labeling
- Updated [tests/test_run_sim_cli.py](/home/tk/Desktop/Architect/tests/test_run_sim_cli.py) with an end-to-end regression that exercises a grouped single-run manifest through a relative program path.
- Updated [README.md](/home/tk/Desktop/Architect/README.md) to document grouped manifests for both sweep and single-run usage.

## Current Roadblocks

- `git push origin main` is still blocked by environment DNS failure for `github.com`.

## Current Questions

- None at the moment.

## Pending Design Decisions

- Whether the next experiment-management step should add manifest-driven artifact path control, custom metric extraction for sweep reports, or stricter validation for manifest-provided config names.

## Next-Step Plan

- Commit this generalized experiment-manifest slice.
- Attempt the required push to remote.
- Continue with the next experiment-management feature after the push attempt.
