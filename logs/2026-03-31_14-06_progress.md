# Progress Log

## Timestamp

2026-03-31 14:06:00 PDT

## Current Progress

- `scripts/run_sim.py` now accepts assembly sources directly and assembles them to a transient ELF before decode/run.
- The RV32I ISA coverage has been consolidated into one authoritative test module at `tests/test_rv32i.py`.
- Bare relative CLI artifact outputs now default under the repository `out/` directory.

## Current Implementation Status

- The CLI program positional argument now accepts raw binaries, ELF32 images, and assembly sources based on file suffix.
- `tests/test_run_sim_cli.py` now covers direct assembly input and default `out/` output placement.
- `tests/test_rv32i_engine.py` and `tests/test_rv32ui_rewritten.py` were retired after their coverage moved into `tests/test_rv32i.py`.
- Full validation is green with `uv run python -m compileall perf_modeling tests scripts toolchains` and `uv run pytest tests -q` at 103 passing tests.

## Current Roadblocks

- `git push origin main` is still expected to fail in this environment because DNS resolution to `github.com` remains blocked.
- The worktree still contains unrelated user-side changes: deleted `main.py`, deleted `tests/isa/README.md`, and untracked `scripts/codex.py`.

## Current Questions

- No open technical questions are currently tracked.

## Pending Design Decisions

- Whether default CLI artifacts should remain repo-rooted under `out/` or become configurable via an explicit output-directory flag.
- Whether the next thread should keep expanding CLI usability or pivot back into simulator timing/model fidelity.

## Next-Step Plan

- Commit the assembly-input, output-defaulting, and ISA-test consolidation slice.
- Attempt the required remote push again and record the expected failure if network restrictions persist.
- Continue into the next unchecked or newly added instruction after checkpointing this milestone.
