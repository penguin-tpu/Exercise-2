# Progress Log

## Timestamp

2026-03-31 12:11:28 PDT

## Current Progress

- Extended the repo-local GNU RISC-V setup from binutils-only to a compiler-driver flow by unpacking `gcc-riscv64-unknown-elf` into the same `toolchains/riscv32/gnu-binutils/` cache.
- Switched `toolchains/riscv32/assemble_to_elf.py` to compile and link through `riscv64-unknown-elf-gcc`.
- Added a preprocessing-sensitive toolchain regression so `.S` sources are validated through the GNU compiler driver rather than only raw assembly syntax.

## Current Implementation Status

- `toolchains/riscv32/gnu_toolchain.py` now resolves `riscv64-unknown-elf-gcc` together with GNU assembler/linker/binutils paths.
- Repo-local bootstrap now downloads and unpacks both `binutils-riscv64-unknown-elf` and `gcc-riscv64-unknown-elf`.
- `tests/test_toolchain_wrapper.py` now covers:
  - basic ELF generation
  - GNU preprocessor support
  - local-label branch flow
  - scratchpad-mapped load/store behavior
- Full verification remains green:
  - `python3 -m compileall perf_modeling tests scripts toolchains main.py`
  - `uv run pytest tests -q`
  - `uv run python scripts/run_sim.py /tmp/rv32_smoke_from_gcc_wrapper.elf`

## Current Roadblocks

- `git push` remains blocked by DNS/network resolution to GitHub.
- The repo-local toolchain is still sourced from distro packages, not a source build of `riscv-gnu-toolchain`.

## Current Questions

- Whether the next toolchain step should source-build `riscv-gnu-toolchain` under `toolchains/` or keep the current repo-local packaged GNU toolchain as the practical baseline.
- Whether the next simulator step should focus on broader ISA compliance or return to accelerator-side instruction/timing depth.

## Pending Design Decisions

- Whether to commit to the packaged repo-local GNU toolchain bootstrap as the permanent developer workflow.
- Whether to add more `.S` tests that depend on preprocessing/macros now that the GCC driver path is available.

## Next-Step Plan

- Commit the compiler-driver upgrade as a separate milestone.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue with the next architectural/performance milestone after the toolchain checkpoint is recorded.
