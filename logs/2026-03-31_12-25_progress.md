# Progress Log

## Timestamp

2026-03-31 12:25:33 PDT

## Current Progress

- Expanded the handwritten ISA corpus to cover the remaining missing RV32I base opcodes.
- Added a machine-checked coverage assertion so the ISA suite now fails if the base RV32I opcode union becomes incomplete.
- Revalidated the full repository test suite after the ISA expansion.

## Current Implementation Status

- New ISA sources were added for the previously uncovered instructions:
  - `and.S`
  - `ecall.S`
  - `fence.S`
  - `lh.S`
  - `slt.S`
- `beq.S` now also exercises `bge` and `bltu`.
- `tests/test_rv32ui_rewritten.py` now tracks the opcode coverage of each source and asserts complete RV32I base coverage as part of the test suite.
- Full verification is currently green with `uv run pytest tests -q` at 36 passing tests.

## Current Roadblocks

- `git push` is still blocked by DNS/network resolution to GitHub.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, `INSTRUCTIONS.md`, the deleted `main.py`, the untracked `scripts/codex.py`, and the untracked `docs/` tree.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether the next milestone should focus on deeper microarchitectural timing behavior or further ELF/program-level compliance.
- Whether the ISA coverage manifest should remain hand-maintained or evolve into automated assembly parsing later.

## Next-Step Plan

- Commit the complete-RV32I ISA coverage slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next simulator milestone after checkpointing these tests.
