# Progress Log

## Timestamp

2026-03-31 11:51:40 PDT

## Current Progress

- Started the toolchain item from `INSTRUCTIONS.md` and implemented a local no-linker RV32I assembly wrapper under `toolchains/riscv32/`.
- Added a script that assembles RV32I assembly with `clang`, extracts the `.text` section from the relocatable object, and wraps it into a minimal ELF32 executable that the simulator can consume.
- Added an end-to-end test that builds a tiny assembly program through the wrapper and executes the resulting ELF in the simulator.

## Current Implementation Status

- New local toolchain support now available:
  - `toolchains/riscv32/assemble_to_elf.py`
  - `toolchains/riscv32/README.md`
- This path works for self-contained `.S` files without relocation sections.
- Verified commands:
  - `python3 toolchains/riscv32/assemble_to_elf.py /tmp/minimal_rv32i.S --output /tmp/minimal_rv32i.elf`
  - `uv run python scripts/run_sim.py /tmp/minimal_rv32i.elf`
  - `uv run python -m unittest tests.test_rv32i tests.test_rv32i_engine tests.test_tensor_slice tests.test_dma_slice tests.test_toolchain_wrapper`

## Current Roadblocks

- The wrapper is intentionally not a full linker; it rejects relocation sections and is aimed at self-contained assembly only.
- Remote pushes remain blocked by the current environment’s network restriction.
- `pytest` is still unavailable in the environment, so the test-harness migration item remains blocked on dependency/tooling availability.

## Current Questions

- Whether the next step on the toolchain side should add convenience wrappers for source templates and output directories, or hold the line at the current self-contained-assembly scope until a fuller linker is available.
- Whether to start translating the current `unittest` suite to pytest-style source while leaving execution under `unittest`, or defer that until `pytest` itself is installable.

## Pending Design Decisions

- How much complexity to add to the local no-linker wrapper before it stops being a maintainable stopgap.
- Whether to surface the toolchain wrapper through `scripts/` as another user-facing entrypoint once it stabilizes.

## Next-Step Plan

- Keep checking `INSTRUCTIONS.md` and only mark the next item once active work starts.
- Evaluate the least fragile path for the `pytest` migration under the current environment constraints.
- Continue extending accelerator-side builders and tests while environment-limited tasks remain partially blocked.
