# Progress Log

## Timestamp

2026-03-31 12:50:51 PDT

## Current Progress

- Added staged workload smoke builders that DMA tensors through scratchpad before compute and DMA results back out to DRAM.
- Added end-to-end regressions for staged vector-add and staged matmul flows.
- Revalidated the full repository test suite after the workload-layer expansion.

## Current Implementation Status

- `perf_modeling/workloads/builders.py` now provides:
  - `build_staged_vector_add_smoke_test(...)`
  - `build_staged_matmul_smoke_test(...)`
- These builders stage inputs into scratchpad with DMA, fence before compute, execute tensor loads and compute, store results back into scratchpad, DMA results out, and fence before exit.
- `tests/test_workload_builders.py` now validates both staged flows end to end, including DMA issue counts and final DRAM results.
- Full verification is green with `uv run pytest tests -q` at 51 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, the deleted `main.py`, the untracked `docs/` tree, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether staged builders should evolve into more general tiling/kernel descriptors instead of fixed smoke-test flows.
- Whether DMA source and destination reservations should still be decomposed into more detailed service windows in a later timing pass.

## Next-Step Plan

- Commit the staged workload-builder slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next model refinement after checkpointing this update.
