# Progress Log

## Timestamp

2026-03-31 12:08:15 PDT

## Current Progress

- Replaced the temporary clang-based RV32I wrapper with a GNU binutils flow under `toolchains/riscv32/`.
- Bootstrapped repo-local `riscv64-unknown-elf` binutils without root by downloading and unpacking the Debian package under `toolchains/riscv32/gnu-binutils/`.
- Added handwritten RV32I ISA-style regression programs under `tests/isa/` and a pytest harness that assembles and runs them through the simulator.
- Kept the simulator execution path stable while expanding the external toolchain and ISA validation coverage.

## Current Implementation Status

- `toolchains/riscv32/assemble_to_elf.py` now assembles with GNU `as` and links with GNU `ld`.
- `toolchains/riscv32/gnu_toolchain.py` resolves repo-local or host GNU binutils and can bootstrap the repo-local copy when missing.
- `toolchains/riscv32/bootstrap_gnu_binutils.py` provides an explicit setup entrypoint under `toolchains/`.
- `tests/test_toolchain_wrapper.py` passes against the GNU path.
- `tests/test_rv32ui_rewritten.py` passes against five handwritten RV32I assembly programs covering arithmetic, control flow, branches, shifts/logic, and memory behavior.

## Current Roadblocks

- `git push` is still expected to fail because outbound network/DNS to GitHub remains blocked in this environment.
- The local GNU setup currently vendors binutils only; there is still no repo-local GNU C compiler or libc stack.

## Current Questions

- Whether the next toolchain step should add a repo-local GCC/bootstrap path or stop at the assembly-and-link flow now that handwritten ISA tests are running.
- Whether the next simulator milestone should return to accelerator-side execution depth or broaden architectural compliance further.

## Pending Design Decisions

- How much of the RISC-V GNU toolchain to vendor locally versus bootstrap on demand.
- Whether to grow the rewritten ISA suite primarily by instruction-category breadth or by more timing-observability assertions.

## Next-Step Plan

- Commit the GNU toolchain transition and the rewritten RV32I ISA regression suite as one milestone.
- Attempt the required `git push` and record the expected network failure if it persists.
- Continue from the next highest-value architectural/performance milestone after the repo is checkpointed.
