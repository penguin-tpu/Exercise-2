# Progress Log

## Timestamp

2026-03-31 12:21:53 PDT

## Current Progress

- Restructured the handwritten ISA regression corpus into individual test sources such as `addi.S`, `add.S`, `beq.S`, `jal.S`, `lw.S`, and `sll.S` to match the user-requested `rv32ui`-style layout more closely.
- Strengthened the bare-metal toolchain validation by asserting that generated ELFs are freestanding and have no dynamic section.
- Revalidated the full repository test suite after both changes.

## Current Implementation Status

- `tests/isa/` now uses a per-test layout rather than grouped source blobs.
- `tests/test_rv32ui_rewritten.py` now targets the individual `.S` files in `tests/isa/`.
- `tests/test_toolchain_wrapper.py` now verifies:
  - GNU preprocessing
  - branch/control flow
  - scratchpad memory access
  - ELF `.data` and `.bss`
  - trap-handler plus `mret`
  - static bare-metal ELF generation with no dynamic section
- `toolchains/riscv32/gnu_toolchain.py` now resolves `readelf` for ELF-property validation.
- Full verification is currently green with `uv run pytest tests -q` at 30 passing tests.

## Current Roadblocks

- `git push` is still blocked by DNS/network resolution to GitHub.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, `INSTRUCTIONS.md`, the deleted `main.py`, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether the next milestone should continue ISA/compliance breadth or return to deeper accelerator scheduling and performance behavior.
- Whether the wrapper should stay fixed at `rv32i_zicsr` or grow an explicit ISA-selection CLI once the current requirements stabilize.

## Next-Step Plan

- Commit the per-test ISA layout and bare-metal ELF validation as the next milestone.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next simulator-core milestone after checkpointing these changes.
