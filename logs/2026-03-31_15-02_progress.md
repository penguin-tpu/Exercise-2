# Progress Log

## Current Progress

- Added the architecture specification document under `docs/specs/`.
- Added the microarchitecture specification document under `docs/specs/`.
- Marked the final two checklist items in `INSTRUCTIONS.md` as started/completed.

## Current Implementation Status

- `docs/specs/architecture_specification.md` now captures the programmer-visible machine model:
  - program formats
  - memory map
  - supported RV32I surface
  - machine-level extensions
  - accelerator-side placeholder operations
  - configuration presets
  - representative workloads
- `docs/specs/microarchitecture_specification.md` now captures the internal timing model:
  - cycle flow
  - event queue
  - issue rules
  - scoreboards
  - memory contention and reservations
  - latency formulas
  - fetch-stall accounting
  - trace and reporting surfaces
- `INSTRUCTIONS.md` is now fully checked off.

## Current Roadblocks

- Remote push remains blocked by environment DNS resolution failure for `github.com`.

## Current Questions

- None.

## Pending Design Decisions

- Whether future documentation should add a generated index that links README, specs, interval log summaries, and workload examples together.
- Whether accelerator-side placeholder operations should eventually get their own public ISA document once a textual or binary frontend exists for them.

## Next-Step Plan

- Maintain the new spec documents as the simulator evolves so they remain synchronized with the actual code paths.
- If more work is requested, build on top of the now-complete instruction checklist rather than reopening already completed items.
