# Progress Log

## Timestamp

2026-03-31 13:50:00 PDT

## Current Progress

- Expanded the handwritten RV32I ISA corpus again, this time for the remaining register-register mixed cases.
- Split `sub`, `xor`, `srl`, `sra`, and `sltu` into dedicated handwritten sources instead of inheriting them from mixed arithmetic files.
- Narrowed the old `add.S`, `and.S`, and `slt.S` sources so their contents now match the opcodes they claim to cover.

## Current Implementation Status

- New dedicated ISA sources now exist under `tests/isa/` for `sub.S`, `xor.S`, `srl.S`, `sra.S`, and `sltu.S`.
- `tests/isa/add.S` now focuses on `add`, `tests/isa/and.S` now focuses on `and`, and `tests/isa/slt.S` now focuses on `slt`.
- `tests/test_rv32ui_rewritten.py` now maps the new register-register opcodes to dedicated sources instead of piggybacking them from older mixed files.
- Full verification is green with `uv run pytest tests -q` at 90 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in the deleted `main.py` and the untracked `scripts/codex.py`.

## Current Questions

- No open technical questions are currently tracked in `QUESTIONS.md`.

## Pending Design Decisions

- Whether the next ISA cleanup slice should split out the remaining memory and system opcodes (`lui`, `lb`, `lbu`, `lhu`, `sb`, `sh`, `sw`, `ebreak`) into fully dedicated sources as well.
- Whether to stop after this slice or keep pushing all the way to a stricter one-source-per-opcode corpus.

## Next-Step Plan

- Commit the dedicated register-register ISA coverage slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next ISA cleanup slice after checkpointing this update.
