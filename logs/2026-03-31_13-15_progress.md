# Progress Log

## Timestamp

2026-03-31 13:15:00 PDT

## Current Progress

- Added peak queue-depth tracking per unit while preserving the existing queue-occupancy histograms.
- Extended the CLI `units` report so it now exposes `max_queue_occupancy` alongside issued-op and busy-cycle totals.
- Revalidated both the focused DMA/CLI tests and the full repository suite after the reporting refinement.

## Current Implementation Status

- `perf_modeling/stats.py` now records `unit.max_queue_occupancy` whenever queue-depth samples are taken.
- `scripts/run_sim.py` now prints `max_queue_occupancy` in the curated `--report units` view.
- `tests/test_dma_slice.py` validates that overlapping DMA transfers raise the DMA peak queue depth to 2.
- `tests/test_run_sim_cli.py` validates the new unit-report field through both the subprocess CLI path and the shared report helper.
- Full verification is green with `uv run pytest tests -q` at 58 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, the deleted `main.py`, the untracked `docs/` tree, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether unit reporting should later add utilization percentages derived from cycles and busy cycles.
- Whether occupancy reporting should eventually grow percentile or tail-depth summaries in addition to the histogram and peak depth.

## Next-Step Plan

- Commit the peak queue-depth reporting slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next simulator stats/reporting refinement after checkpointing this update.
