# Progress Log

## Timestamp

2026-03-31 12:56:15 PDT

## Current Progress

- Added queue-occupancy histogram sampling for modeled units.
- Added a DMA regression that proves the histogram captures multiple in-flight DMA transfers.
- Revalidated the full repository suite after the stats-sampling change.

## Current Implementation Status

- `perf_modeling/stats.py` now records sampled queue-occupancy buckets as counters like `dma.queue_occupancy.2`.
- `perf_modeling/engine.py` now samples each unit’s `queued_ops` value every cycle during stats collection.
- `tests/test_dma_slice.py` now validates that back-to-back DMA copies populate both the single-inflight and two-inflight occupancy buckets.
- Full verification is green with `uv run pytest tests -q` at 52 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, the deleted `main.py`, the untracked `docs/` tree, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether occupancy reporting should eventually include time-series exports or only histogram buckets.
- Whether DMA source and destination reservations should still be decomposed into more detailed service windows in a later timing pass.

## Next-Step Plan

- Commit the queue-occupancy histogram slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next simulator/reporting refinement after checkpointing this update.
