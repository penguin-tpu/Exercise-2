# Progress Log

## Timestamp

2026-03-31 12:14:48 PDT

## Current Progress

- Added one stronger GNU-toolchain regression that exercises `.data`, `.bss`, symbol relocation, and zero-initialized ELF data through the simulator.
- Added workload-builder helpers for vector-add and matmul smoke programs on top of the tensor slice.
- Added end-to-end tests that execute those builder-generated programs through the simulator and validate both results and fence behavior.
- Recorded the open toolchain clarification in `QUESTIONS.md` per the updated `AGENTS.md` rule.

## Current Implementation Status

- `tests/test_toolchain_wrapper.py` now covers:
  - basic ELF generation
  - GNU preprocessor support
  - local-label branches
  - scratchpad-mapped loads/stores
  - ELF `.data` plus `.bss` behavior
- `perf_modeling/workloads/builders.py` now exposes:
  - `build_vector_add_smoke_test`
  - `build_matmul_smoke_test`
- `tests/test_workload_builders.py` validates both new builders end to end.
- Full verification remains green with `uv run pytest tests -q` at 26 passing tests.

## Current Roadblocks

- `git push` is still blocked by DNS/network resolution to GitHub.
- `main.py` has been deleted in the current worktree by another thread or by the user, so verification is now anchored on `scripts/run_sim.py` and package-level compile checks instead of restoring the compatibility shim unasked.

## Current Questions

- Whether the packaged repo-local GNU toolchain is acceptable, or whether the repository specifically needs a source-built `riscv-gnu-toolchain` checkout under `toolchains/`.

## Pending Design Decisions

- Whether to preserve the current packaged GNU-toolchain bootstrap as the long-term developer workflow.
- Whether the next simulator milestone should emphasize more ISA/privilege coverage or deeper accelerator-kernel scheduling behavior.

## Next-Step Plan

- Commit the ELF-segment regression, workload-builder helpers, and `QUESTIONS.md` note as the next milestone.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next simulator-core milestone after checkpointing these additions.
