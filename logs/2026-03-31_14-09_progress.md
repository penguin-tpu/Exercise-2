# Progress Log

## Timestamp

2026-03-31 14:09:13 PDT

## Current Progress

- Added a DRAM dump path to the simulator CLI alongside the existing scratchpad dump support.
- Extended the CLI regression suite to validate DRAM dump output from a real assembly program.
- Revalidated the full repository suite after the new CLI artifact path landed.

## Current Implementation Status

- `scripts/run_sim.py` now supports `--dram-dump`, `--dram-dump-offset`, and `--dram-dump-size`.
- The DRAM dump path reuses the same output-path normalization logic as the other CLI artifact exports.
- `tests/test_run_sim_cli.py` now synthesizes a temporary RV32I assembly program, stores a word to DRAM, and verifies the dumped bytes.
- Full verification is green with `uv run python -m compileall perf_modeling tests scripts toolchains` and `uv run pytest tests -q` at 104 passing tests.

## Current Roadblocks

- `git push origin main` is still expected to fail because DNS/network resolution to `github.com` remains blocked in this environment.
- The worktree still contains unrelated user-side changes: deleted `main.py`, deleted `tests/isa/README.md`, and untracked `scripts/codex.py`.

## Current Questions

- No open technical questions are currently tracked.

## Pending Design Decisions

- Whether the CLI should eventually expose a higher-level unified output manifest instead of separate dump/export flags.
- Whether the next slice should stay on CLI usability or pivot back into simulator timing/model fidelity.

## Next-Step Plan

- Commit the DRAM dump support slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next simulator milestone after checkpointing this update.
