# Progress Log

## Timestamp

2026-03-31 13:08:05 PDT

## Current Progress

- Extended the curated CLI report surface with memory and contention views.
- Added direct report-helper coverage for those new report modes while preserving the subprocess CLI tests.
- Revalidated the full repository suite after the reporting refinement.

## Current Implementation Status

- `scripts/run_sim.py` now supports:
  - `--report latency`
  - `--report occupancy`
  - `--report memory`
  - `--report contention`
- The memory report prints per-memory byte counters from the flattened stats snapshot.
- The contention report prints aggregate and histogram-style contention counters.
- `tests/test_run_sim_cli.py` now validates:
  - JSON exports
  - filtered stat printing
  - trace-tail printing
  - curated latency and occupancy reports
  - curated memory and contention reports via the shared helper
- Full verification is green with `uv run pytest tests -q` at 58 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, the deleted `main.py`, the untracked `docs/` tree, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether the CLI should later add report modes for staged workloads or ISA coverage summaries in addition to the current stat-family reports.
- Whether DMA source and destination reservations should still be decomposed into more detailed service windows in a later timing pass.

## Next-Step Plan

- Commit the expanded CLI-reporting slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next simulator/reporting refinement after checkpointing this update.
