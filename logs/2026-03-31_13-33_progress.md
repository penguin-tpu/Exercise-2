# Progress Log

## Timestamp

2026-03-31 13:33:00 PDT

## Current Progress

- Added machine-readable CSV export for flat simulator stats.
- Exposed one `--stats-csv` control so the CLI can feed spreadsheet or scripting workflows without JSON parsing.
- Revalidated the CLI-specific path and the full repository suite after the export refinement.

## Current Implementation Status

- `scripts/run_sim.py` now accepts `--stats-csv`.
- The CSV export writes a simple two-column `key,value` table over the flat stats snapshot and supports file output or stdout just like the JSON export path.
- `tests/test_run_sim_cli.py` now validates the CSV export through the subprocess path.
- Full verification is green with `uv run pytest tests -q` at 67 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, the deleted `main.py`, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether later CLI/export work should add CSV export for trace records in addition to stats.
- Whether the next thread should pivot from reporting/export refinements back into deeper simulator timing/modeling behavior.

## Next-Step Plan

- Commit the CSV-export CLI slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next simulator/reporting refinement after checkpointing this update.
