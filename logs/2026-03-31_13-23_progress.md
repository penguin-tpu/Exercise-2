# Progress Log

## Timestamp

2026-03-31 13:23:00 PDT

## Current Progress

- Added derived byte-share summaries to the CLI memory report.
- Exposed total read and write traffic plus per-key percentages without changing simulator-core stats generation.
- Revalidated the CLI-specific path and the full repository suite after the reporting refinement.

## Current Implementation Status

- `scripts/run_sim.py` now prints `report memory_summary ...` lines for read and write totals before per-key memory traffic lines.
- Each per-key memory line now includes a derived `pct=...` field relative to the total bytes for that direction.
- `tests/test_run_sim_cli.py` now validates both the builtin zero-byte subprocess path and a synthetic multi-layer byte-share helper path.
- Full verification is green with `uv run pytest tests -q` at 61 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, the deleted `main.py`, the untracked `docs/` tree, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether the contention report should next add derived percentages rather than only raw counters.
- Whether later CLI reporting should grow a top-level summary mode that combines pipeline, unit, latency, and memory highlights.

## Next-Step Plan

- Commit the memory-share reporting slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next reporting refinement after checkpointing this update.
