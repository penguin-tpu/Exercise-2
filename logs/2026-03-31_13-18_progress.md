# Progress Log

## Timestamp

2026-03-31 13:18:00 PDT

## Current Progress

- Added a curated stall-focused report to the simulator CLI.
- Exposed both a total stall summary and per-stall-category lines so issue bottlenecks are visible without raw stat-prefix filtering.
- Revalidated the CLI-specific path and the full repository suite after the report expansion.

## Current Implementation Status

- `scripts/run_sim.py` now supports `--report stalls`.
- The stall report prints `report stalls_summary total=... categories=...` followed by `report stalls key=... value=...` lines.
- `tests/test_run_sim_cli.py` now validates both the builtin zero-stall subprocess path and a synthetic nonzero helper path for stall reporting.
- Full verification is green with `uv run pytest tests -q` at 60 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, the deleted `main.py`, the untracked `docs/` tree, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether unit reports should next add utilization percentages derived from busy cycles and total cycles.
- Whether later CLI reporting should add grouped pipeline summaries that combine issue, retire, stall, and contention views.

## Next-Step Plan

- Commit the stall-report CLI slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next reporting refinement after checkpointing this update.
