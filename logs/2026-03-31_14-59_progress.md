# Progress Log

## Current Progress

- Added several representative runnable workload examples under `tests/workload/`.
- Extended the workload-example regression file so each example assembles and executes end to end through the existing toolchain and simulator path.
- Marked the workload-example checklist item as started/completed in `INSTRUCTIONS.md`.

## Current Implementation Status

- `tests/workload/` now includes:
  - `scalar_int_matmul.S`
  - `scalar_vector_add.S`
  - `scalar_reduce_sum.S`
  - `scalar_relu.S`
- `tests/test_workload_examples.py` now assembles and runs each of those examples and verifies the expected scratchpad outputs and exit codes.
- The new examples cover representative scalar kernel shapes:
  - elementwise vector add
  - reduction
  - elementwise clamp or ReLU
  - matmul via repeated-add multiply loops
- Full verification remains green after adding the new example programs.

## Current Roadblocks

- Remote push remains blocked by environment DNS resolution failure for `github.com`.

## Current Questions

- None.

## Pending Design Decisions

- Whether the next unchecked slice should start with the architecture specification or the microarchitecture specification.
- Whether future workload examples should stay RV32I-scalar-only until accelerator opcodes gain a textual frontend path, or whether synthetic builder-backed examples should also be documented alongside the assembly set.

## Next-Step Plan

- Continue with the remaining unchecked documentation/specification items.
- Keep the workload examples aligned with the CLI and README so users can run them directly as reference programs.
