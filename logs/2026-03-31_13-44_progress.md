# Progress Log

## Timestamp

2026-03-31 13:44:00 PDT

## Current Progress

- Expanded the handwritten RV32I ISA corpus again, this time for the immediate ALU and shift family.
- Split `slti`, `sltiu`, `xori`, `ori`, `andi`, `slli`, `srli`, and `srai` into dedicated handwritten sources instead of inheriting them from mixed files.
- Repurposed `sll.S` so it now actually tests `sll`, and trimmed stale opcode claims from the old mixed sources.

## Current Implementation Status

- New dedicated ISA sources now exist under `tests/isa/` for `andi.S`, `or.S`, `ori.S`, `slli.S`, `slti.S`, `sltiu.S`, `srai.S`, `srli.S`, and `xori.S`.
- `tests/isa/addi.S` now focuses on `addi`, and `tests/isa/sll.S` now focuses on `sll`.
- `tests/test_rv32ui_rewritten.py` now maps these opcodes to their dedicated files instead of the older piggyback coverage declarations.
- Full verification is green with `uv run pytest tests -q` at 84 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in the deleted `main.py` and the untracked `scripts/codex.py`.

## Current Questions

- No open technical questions are currently tracked in `QUESTIONS.md`.

## Pending Design Decisions

- Whether the next ISA cleanup slice should split out the remaining register-register mixed cases (`sub`, `xor`, `srl`, `sra`, `sltu`) into dedicated files as well.
- Whether to continue driving toward one-source-per-opcode rigor or stop once the highest-risk metadata false positives are removed.

## Next-Step Plan

- Commit the dedicated immediate/shift ISA coverage slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next ISA cleanup slice after checkpointing this update.
