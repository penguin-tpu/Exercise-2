# Progress Log

## Timestamp

2026-03-31 11:50:00 PDT

## Current Progress

- Added an asynchronous DMA placeholder instruction that moves bytes through the existing event queue and DMA unit.
- Added a DMA overlap test showing that scalar issue can proceed while DMA is in flight and that `fence` waits for completion.
- Moved the simulator CLI implementation under `scripts/` while keeping `main.py` as a compatibility wrapper.
- Updated `INSTRUCTIONS.md` to mark the script-move item as started and in progress.

## Current Implementation Status

- New accelerator-side functionality now includes:
  - `dma_copy` issued through the DMA unit with setup-plus-bandwidth timing
  - builder support for DMA copies and a concrete DMA smoke-test workload builder
  - direct script entry at `scripts/run_sim.py`
  - backward-compatible root entry at `main.py`
- Verification is green after the script move and DMA addition:
  - `python3 -m compileall perf_modeling tests scripts main.py`
  - `uv run python scripts/run_sim.py`
  - `uv run python -m unittest tests.test_rv32i tests.test_rv32i_engine tests.test_tensor_slice tests.test_dma_slice`

## Current Roadblocks

- Remote push remains blocked by the current environment’s network restriction.
- `pytest` is not installed in the current environment, so the test-harness refactor item in `INSTRUCTIONS.md` is not yet directly actionable without adding dependencies.
- A full local RISC-V toolchain is still missing a linker path, so the toolchain/setup item in `INSTRUCTIONS.md` remains constrained by the current environment.

## Current Questions

- Whether the next user-priority item should be handled by building local wrapper tooling around the partial RISC-V toolchain already present, or by continuing to strengthen the simulator architecture while waiting for a fuller toolchain.
- Whether the `pytest` migration should be staged as a source-level refactor first, with dependency install deferred, or held until the environment can actually run `pytest`.

## Pending Design Decisions

- How to package any local toolchain wrappers under `./toolchains/` without pretending the missing linker does not matter.
- Whether `main.py` should eventually be removed after downstream workflows switch to `scripts/run_sim.py`, or retained indefinitely as a stable compatibility shim.

## Next-Step Plan

- Decide the most defensible partial implementation for the toolchain item under the current environment limits.
- Continue checking `INSTRUCTIONS.md` and only mark additional entries when they are actively being worked.
- Keep extending the accelerator-side program builders and tests while the environment-limited items are being evaluated.
