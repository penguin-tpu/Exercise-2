# Progress Log

## Timestamp

2026-03-31 12:57:55 PDT

## Current Progress

- Added per-memory byte accounting so DRAM and scratchpad traffic are visible directly in the stats surface.
- Extended DMA and tensor regressions to validate those per-memory byte counters.
- Revalidated the full repository suite after the planner/stats refinement.

## Current Implementation Status

- `perf_modeling/isa/semantics.py` now emits per-memory byte counters such as:
  - `dram.bytes_read`
  - `dram.bytes_written`
  - `scratchpad.bytes_read`
  - `scratchpad.bytes_written`
- The counters are emitted by scalar loads/stores, tensor loads/stores, and DMA copies.
- `tests/test_dma_slice.py` now validates DRAM-to-scratchpad byte accounting for DMA.
- `tests/test_tensor_slice.py` now validates DRAM and scratchpad byte accounting for the tensor vertical slice.
- Full verification is green with `uv run pytest tests -q` at 52 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, the deleted `main.py`, the untracked `docs/` tree, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether memory accounting should eventually split DMA setup overhead from transfer bytes in the public stats surface.
- Whether DMA source and destination reservations should still be decomposed into more detailed service windows in a later timing pass.

## Next-Step Plan

- Commit the per-memory byte-accounting slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next simulator/stats refinement after checkpointing this update.
