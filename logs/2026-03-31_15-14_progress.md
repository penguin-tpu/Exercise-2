# Progress Log

## Current Progress

- Added multi-config sweep support to the simulator CLI for lightweight design-space exploration.
- One program can now be executed across several named hardware presets from a single command, with optional JSON export of all sweep results.

## Current Implementation Status

- Extended [scripts/run_sim.py](/home/tk/Desktop/Architect/scripts/run_sim.py) with:
  - `--sweep-config` for repeatable named preset selection
  - `--sweep-json` for structured multi-run result export
  - validation to keep sweep mode separate from single-run-only artifact and report flags
- Sweep mode now:
  - decodes the input program once
  - reapplies DRAM and scratchpad preload images to each run
  - executes one run per named config
  - prints one summary line per config
  - emits JSON containing config name, config snapshot, halt status, exit code, summary, and flat stats per run
- Updated [tests/test_run_sim_cli.py](/home/tk/Desktop/Architect/tests/test_run_sim_cli.py) with:
  - an end-to-end sweep regression across `baseline` and `tiny_debug`
  - a validation regression that rejects ambiguous `--report` usage during sweep mode
- Updated [README.md](/home/tk/Desktop/Architect/README.md) with the new sweep options and example usage.

## Current Roadblocks

- `git push origin main` is still blocked by environment DNS failure for `github.com`.

## Current Questions

- None at the moment.

## Pending Design Decisions

- Whether the next DSE-facing step should be CSV export for sweeps, a ranking/sort mode on selected metrics, or grouped experiment manifests that combine workload path, memory preloads, and config lists.

## Next-Step Plan

- Commit this sweep-mode CLI slice.
- Attempt the required push to remote.
- Continue with the next small experiment-management or reporting feature after the push attempt.
