# Progress Log

## Timestamp

2026-03-31 12:29:25 PDT

## Current Progress

- Added explicit stall trace records to the engine so timing stalls are visible in the execution trace, not just in aggregate counters.
- Added focused tests that verify trace visibility for scalar dependency stalls and fence waits behind asynchronous DMA work.
- Revalidated the full repository suite after the trace-observability change.

## Current Implementation Status

- `perf_modeling/engine.py` now emits `stall` trace records for:
  - scalar dependency stalls
  - scalar WAW stalls
  - tensor dependency stalls
  - tensor WAW stalls
  - CSR dependency stalls
  - CSR WAW stalls
  - fence waits
  - unit-busy stalls
- `tests/test_trace_visibility.py` validates both dependency and fence stall trace behavior.
- Full verification is currently green with `uv run pytest tests -q` at 38 passing tests.

## Current Roadblocks

- `git push` is still blocked by DNS/network resolution to GitHub.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, the deleted `main.py`, the untracked `docs/` tree, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether the next milestone should deepen the timing model itself, such as memory banking or contention, now that stall visibility is present.
- Whether the trace should eventually respect the existing `TraceConfig.keep_cycle_trace` and `keep_event_trace` toggles more explicitly.

## Next-Step Plan

- Commit the stall-trace observability slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next timing-model milestone after checkpointing these changes.
