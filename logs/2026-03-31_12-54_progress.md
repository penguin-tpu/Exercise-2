# Progress Log

## Timestamp

2026-03-31 12:54:16 PDT

## Current Progress

- Extended the new contention reporting with per-resource histograms for scratchpad banks, scratchpad ports, and memory resources.
- Updated the contention regressions to verify those per-bank and per-port counters.
- Revalidated the full repository suite after the reporting refinement.

## Current Implementation Status

- `perf_modeling/engine.py` now records histogram-style counters such as:
  - `memory.contention.resource.mem_dram`
  - `scratchpad.bank_conflict.sp_bank_0`
  - `scratchpad.port_conflict.sp_read_port_0`
  - `scratchpad.port_conflict.sp_write_port_0`
- `tests/test_memory_contention.py` now validates those per-resource counters alongside the aggregate contention totals.
- Full verification is green with `uv run pytest tests -q` at 51 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, the deleted `main.py`, the untracked `docs/` tree, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether contention reporting should evolve into richer histograms or sampled occupancy traces beyond simple counter buckets.
- Whether DMA source and destination reservations should still be decomposed into more detailed service windows in a later timing pass.

## Next-Step Plan

- Commit the contention-histogram reporting slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next simulator/reporting refinement after checkpointing this update.
