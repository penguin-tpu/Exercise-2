# Progress Log

## Timestamp

2026-03-31 11:59:54 PDT

## Current Progress

- Strengthened the local RV32I wrapper by resolving supported `.text` relocations instead of dropping them.
- Added toolchain-backed pytest coverage for:
  - a minimal halt program
  - a local-label branch loop
  - scratchpad-mapped load/store assembly
- Removed the persistent Torch startup warning by adding NumPy to the project environment.

## Current Implementation Status

- The local wrapper now supports the self-contained assembly scope more honestly:
  - internal branch relocations
  - internal JAL relocations
- Verification is currently clean and warning-free with:
  - `uv run pytest tests -q`
  - `uv run python scripts/run_sim.py /tmp/minimal_rv32i.elf`
- All current `INSTRUCTIONS.md` items remain marked as started because each one now has concrete implementation behind it.

## Current Roadblocks

- Remote push is still blocked by the environment’s network restriction.
- The local toolchain wrapper is still intentionally limited to self-contained assembly and does not replace a full linker.

## Current Questions

- Whether the next wrapper improvement should handle additional safe relocation types or stop at the current self-contained scope.
- Whether the next engineering step should return to simulator functionality or continue developing the external toolchain/testing workflow.

## Pending Design Decisions

- How much more linker-like behavior is justified in the local wrapper before it becomes too brittle.
- Whether to use the untracked `tests/isa/` corpus as part of a future tracked toolchain regression suite after clarifying ownership.

## Next-Step Plan

- Keep the current wrapper scope explicit and stable while evaluating the next most valuable simulator milestone.
- Continue to avoid touching unrelated untracked user files unless they are explicitly folded into the tracked workflow.
- Check `INSTRUCTIONS.md` again before starting any newly added user item.
