# Progress Log

## Timestamp

2026-03-31 13:41:00 PDT

## Current Progress

- Replaced the remaining live raw `python` command invocations with `uv run python` in the active docs and subprocess-based tests.
- Updated the instruction tracking files by marking the new `uv` item as started and clearing the answered toolchain question from `QUESTIONS.md`.
- Expanded the handwritten RV32I ISA corpus with dedicated control-flow sources for `auipc`, `beq`, `bne`, `blt`, `bge`, `bltu`, `bgeu`, and `jalr`, and stopped over-claiming those opcodes from mixed sources.

## Current Implementation Status

- `AGENTS.md`, `toolchains/riscv32/README.md`, and the affected subprocess tests now consistently use `uv run python`.
- `tests/test_rv32ui_rewritten.py` now includes dedicated cases for the new control-flow sources and no longer claims `auipc`, `jalr`, or the branch family from the old mixed files.
- New ISA sources now exist under `tests/isa/` for `auipc.S`, `bne.S`, `blt.S`, `bge.S`, `bltu.S`, `bgeu.S`, and `jalr.S`, and `beq.S` now actually emits `beq`.
- Full verification is green with `uv run pytest tests -q` at 75 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, `INSTRUCTIONS.md`, `QUESTIONS.md`, the deleted `main.py`, and the untracked `scripts/codex.py`.

## Current Questions

- No open technical questions are currently tracked in `QUESTIONS.md`.

## Pending Design Decisions

- Whether the next ISA expansion should split out the immediate ALU and shift instructions into dedicated handwritten sources (`slti`, `sltiu`, `xori`, `ori`, `andi`, `slli`, `srli`, `srai`).
- Whether the next thread should continue ISA-corpus cleanup or pivot back into deeper simulator timing/modeling behavior.

## Next-Step Plan

- Commit the `uv` command cleanup plus dedicated control-flow ISA coverage slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next ISA cleanup slice after checkpointing this update.
