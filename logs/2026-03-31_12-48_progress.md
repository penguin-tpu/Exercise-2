# Progress Log

## Timestamp

2026-03-31 12:48:27 PDT

## Current Progress

- Wired the existing trace-policy config into the engine so tracing can now be disabled or filtered as configured.
- Added focused trace-policy regressions for global tracing disablement, cycle-only trace retention, and event-only trace retention.
- Revalidated the full repository test suite after the engine trace-path change.

## Current Implementation Status

- `perf_modeling/engine.py` now honors:
  - `timing.enable_tracing`
  - `trace.keep_cycle_trace`
  - `trace.keep_event_trace`
- The current policy split is:
  - `stall` records are controlled by `keep_cycle_trace`
  - `issue`, `complete`, and `trap` records are controlled by `keep_event_trace`
  - all trace output is suppressed when `enable_tracing` is false
- `tests/test_trace_config.py` validates the three policy modes above.
- Full verification is green with `uv run pytest tests -q` at 49 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, the deleted `main.py`, the untracked `docs/` tree, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether to add even finer-grained trace categories beyond the current cycle-versus-event split.
- Whether DMA source and destination reservations should still be decomposed into more detailed service windows in a later timing pass.

## Next-Step Plan

- Commit the trace-policy enforcement slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next simulator refinement after checkpointing this update.
