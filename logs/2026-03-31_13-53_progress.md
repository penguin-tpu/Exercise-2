# Progress Log

## Timestamp

2026-03-31 13:53:00 PDT

## Current Progress

- Expanded the handwritten RV32I ISA corpus again, this time for the remaining memory and system opcodes that were still riding on mixed files.
- Added dedicated sources for `lui`, `lb`, `lbu`, `lhu`, `sb`, `sh`, `sw`, and `ebreak`.
- Trimmed the old `lw.S` and `addi.S` metadata so those files no longer claim those opcodes by inheritance.

## Current Implementation Status

- New dedicated ISA sources now exist under `tests/isa/` for `lui.S`, `lb.S`, `lbu.S`, `lhu.S`, `sb.S`, `sh.S`, `sw.S`, and `ebreak.S`.
- `tests/test_rv32ui_rewritten.py` now maps those opcodes to dedicated files, and `lw.S` now only claims `lw`.
- The ISA corpus is now substantially closer to a one-source-per-opcode structure across the RV32I base set.
- Full verification is green with `uv run pytest tests -q` at 98 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in the deleted `main.py` and the untracked `scripts/codex.py`.

## Current Questions

- No open technical questions are currently tracked in `QUESTIONS.md`.

## Pending Design Decisions

- Whether to keep pushing the handwritten corpus all the way to a strict one-source-per-opcode policy, including `lw`, `lh`, `ecall`, and `fence` style cleanup of helper instructions.
- Whether the next step should pivot back into simulator timing/modeling behavior now that the ISA test corpus is much less metadata-driven.

## Next-Step Plan

- Commit the dedicated memory/system ISA coverage slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next instruction-driven slice after checkpointing this update.
