# Progress Log

## Timestamp

2026-03-31 13:20:00 PDT

## Current Progress

- Added derived utilization percentages to the curated CLI unit report.
- Kept the report based on existing simulator stats, with no change to engine behavior or architectural timing.
- Revalidated the CLI-specific path and the full repository suite after the reporting refinement.

## Current Implementation Status

- `scripts/run_sim.py` now prints `busy_pct=...` in each `report units` line.
- The utilization value is derived from `busy_cycles / cycles` using the flat stats snapshot already produced by the engine.
- `tests/test_run_sim_cli.py` now validates both the builtin subprocess path and a synthetic helper path for the utilization field.
- Full verification is green with `uv run pytest tests -q` at 60 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, the deleted `main.py`, the untracked `docs/` tree, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether later reporting should add a grouped pipeline-summary mode that combines issued, retired, stalled, and busy-unit views.
- Whether memory/contention reports should eventually add derived percentages rather than only raw counters.

## Next-Step Plan

- Commit the unit-utilization reporting slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next reporting refinement after checkpointing this update.
