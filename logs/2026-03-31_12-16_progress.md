# Progress Log

## Timestamp

2026-03-31 12:16:51 PDT

## Current Progress

- Extended the GNU wrapper to assemble machine-CSR programs by switching its ISA string from plain `rv32i` to `rv32i_zicsr`.
- Added one real ELF integration test that installs `mtvec`, takes an `ecall`, services it in a trap handler, updates `mepc`, and returns with `mret`.
- Revalidated the full repository test suite after the toolchain ISA-string change.

## Current Implementation Status

- `toolchains/riscv32/assemble_to_elf.py` now emits binaries compatible with the modeled machine-CSR slice.
- `tests/test_toolchain_wrapper.py` now covers:
  - basic ELF generation
  - GNU preprocessor handling
  - branch loops
  - scratchpad loads/stores
  - ELF `.data` and `.bss`
  - trap-handler plus `mret` control flow
- `uv run pytest tests -q` is currently green at 27 passing tests.

## Current Roadblocks

- `git push` is still blocked by DNS/network resolution to GitHub.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, `INSTRUCTIONS.md`, the deleted `main.py`, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification remains in `QUESTIONS.md`: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether to keep broadening ELF/program compliance coverage or return to deeper accelerator-kernel scheduling behavior next.
- Whether to preserve `rv32i_zicsr` as the default wrapper ISA string or add a user-selectable ISA flag later.

## Next-Step Plan

- Commit the GNU-wrapper CSR/trap integration slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue to the next simulator milestone after checkpointing this integration coverage.
