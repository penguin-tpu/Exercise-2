# Log Interval Summary

## Interval

2026-03-31 15:00 PDT to 2026-03-31 17:00 PDT

## Source Logs

- `logs/2026-03-31_15-02_progress.md`
- `logs/2026-03-31_15-03_progress.md`
- `logs/2026-03-31_15-05_progress.md`
- `logs/2026-03-31_15-09_progress.md`
- `logs/2026-03-31_15-14_progress.md`
- `logs/2026-03-31_15-16_progress.md`
- `logs/2026-03-31_15-19_progress.md`
- `logs/2026-03-31_15-22_progress.md`
- `logs/2026-03-31_15-26_progress.md`
- `logs/2026-03-31_15-29_progress.md`
- `logs/2026-03-31_15-32_progress.md`
- `logs/2026-03-31_15-36_progress.md`
- `logs/2026-03-31_15-44_progress.md`
- `logs/2026-03-31_15-49_progress.md`

## Summary

- Closed out the repository-completion checklist:
  - added the architecture specification
  - added the microarchitecture specification
  - added documentation and log indexes
  - added packaged accelerator config presets
  - added representative scalar workload examples
- Extended experiment and report ergonomics:
  - preset discovery with `--list-configs`
  - resolved config snapshots in reports and manifests
  - multi-config sweep mode with JSON and CSV export
  - ranked sweep ordering and top-N limiting
  - grouped sweep and grouped single-run experiment manifests
  - comparative sweep summary and delta reports
  - manifest-owned artifact path control
- Added freestanding C source support:
  - direct `.c` input support in `scripts/run_sim.py`
  - matching C workload examples under `tests/workload/`
  - wrapper flag tuning for RV32I-friendly freestanding C codegen
- Added float32 accelerator regression coverage:
  - float32 tensor load/vector-add/matmul/store vertical-slice coverage
  - float32 workload-builder smoke tests for vector add and matmul
  - float32 staged workload-builder smoke tests for DMA-to-scratchpad vector add and matmul
- Added the next vector-functional slice:
  - backend reduction support for vector reductions
  - new `vrelu` and `vreduce_sum` ISA semantics wired into the execution registry
  - builder support for vector ReLU and vector reduce-sum smoke programs
  - direct tensor-slice and builder end-to-end tests for ReLU and reduction
  - corrected Perfetto track inference for actual simulator opcodes such as `dma_copy`, `vadd`, `vrelu`, `vreduce_sum`, and `matmul`
- Extended the new vector ops through the broader tensor validation surface:
  - added float32 tensor-slice coverage for vector ReLU plus vector reduce-sum
  - added float32 direct builder coverage for vector ReLU and vector reduce-sum
  - added staged DMA-through-scratchpad builder coverage for vector ReLU and vector reduce-sum
  - added staged float32 builder coverage for vector ReLU and vector reduce-sum
- Added Perfetto-compatible trace export:
  - dedicated `perf_modeling/perfetto.py` exporter
  - `--perfetto-trace` CLI artifact output
  - timeline spans from issue/complete pairs
  - instant stall and trap markers
  - summary counter tracks
- Closed the immediate follow-up gaps after the Perfetto and manifest work landed:
  - experiment manifests can now own `perfetto_trace` artifact paths
  - experiment manifests can now own single-run `stats_csv`, `trace_csv`, `scratchpad_dump`, and `dram_dump` artifact paths as well
  - experiment manifests can now own `scratchpad_dump_offset`, `scratchpad_dump_size`, `dram_dump_offset`, and `dram_dump_size` so dump ranges no longer depend on extra CLI flags
  - experiment manifests can now own single-run `reports`, `report_limit`, `report_match`, `print_stats_prefix`, and `print_trace_limit` controls
  - sweep manifests can now own `sweep_reports`, so comparative sweep stdout reporting no longer depends on extra CLI flags either
  - the `config` report now uses the resolved manifest-selected config name instead of the raw CLI default

## Resulting Repository State

- By the end of this interval, the repository had moved from “feature-complete enough to run” into a documented, sweepable, artifact-rich simulator environment with direct assembly and C entry paths.
- Experiment management now supports named config sweeps, grouped manifests, ranked comparisons, and broader reproducible artifact ownership from the CLI.
- Visualization now includes a first Perfetto-compatible export path in addition to the existing human-readable and JSON/CSV report surfaces, and that artifact can be owned from grouped experiment manifests.
- The accelerator-side tensor path now covers vector add, vector ReLU, vector reduce-sum, and matmul with direct and builder-driven regression coverage.
- The staged tensor path now covers vector add, vector ReLU, vector reduce-sum, and matmul across both integer and float32 payloads.

## Current Progress

- Implemented the next tensor functional slice beyond vector add by adding `vrelu` and `vreduce_sum` semantics, builder APIs, and regression coverage.
- Extended that slice through staged DMA/scratchpad builders and float32 regression coverage.
- Verified the slice with `uv run python -m compileall perf_modeling tests`, focused tensor tests, and the full test suite.

## Current Implementation Status

- The tensor backend now supports elementwise math, matmul, and reduction-backed scalarization of tensor values.
- The ISA layer now recognizes `vrelu` and `vreduce_sum` as vector-unit work and schedules them through the existing event/timing path.
- Perfetto export now maps the simulator’s current opcode names onto the intended vector, MXU, and DMA tracks.
- Workload builders now cover direct and staged vector ReLU/reduce-sum flows for both int32 and float32.

## Current Roadblocks

- `git push origin main` is still blocked by DNS resolution failure for `github.com`, so commits remain local.

## Current Questions

- No open technical questions were added in this slice.

## Pending Design Decisions

- The next meaningful backend extension is whether to keep growing bespoke tensor ops (`vrelu`, reductions, more elementwise kernels) or pivot to a higher-level kernel/program ingestion path for PyTorch-oriented workloads.

## Next-Step Plan

- Extend the vector/tensor path with additional reduction or activation coverage that is useful for ML kernels.
- Keep tightening end-to-end coverage so new tensor ops are exercised in both direct vertical slices and builder-generated programs.
