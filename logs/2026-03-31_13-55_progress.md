# Progress Log

## Timestamp

2026-03-31 13:55:00 PDT

## Current Progress

- Added static guardrails around the handwritten RV32I ISA corpus so the metadata can’t drift away from the source files again.
- Enforced one-source-per-opcode case mapping in the rewritten ISA test table.
- Added a direct source-text audit that checks each claimed opcode really appears in the corresponding assembly file.

## Current Implementation Status

- `tests/test_rv32ui_rewritten.py` now has static audits that require each case to claim exactly one opcode and require the case filename stem to match that opcode.
- The same test file now also verifies that each claimed opcode token appears in the corresponding assembly source.
- Full verification is green with `uv run pytest tests -q` at 100 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in the deleted `main.py` and the untracked `scripts/codex.py`.

## Current Questions

- No open technical questions are currently tracked in `QUESTIONS.md`.

## Pending Design Decisions

- Whether to keep pushing the ISA corpus toward even stricter “minimal helper opcodes per file” conventions.
- Whether the next thread should pivot back into simulator timing/modeling behavior now that the ISA corpus and its guardrails are much stronger.

## Next-Step Plan

- Commit the ISA source/metadata guardrail slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next implementation slice after checkpointing this update.
