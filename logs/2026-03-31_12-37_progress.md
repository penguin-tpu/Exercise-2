# Progress Log

## Timestamp

2026-03-31 12:37:28 PDT

## Current Progress

- Wired shared resource reservations into a more realistic memory-contention slice.
- Kept DRAM as a coarse shared layer while refining scratchpad contention to bank-level reservations.
- Added focused regressions that prove same-bank DMA and load/store conflicts stall, while different-bank scratchpad traffic can overlap.

## Current Implementation Status

- `perf_modeling/isa/semantics.py` now emits shared reservations for:
  - `mem_dram` on DRAM accesses
  - `sp_bank_<n>` on scratchpad accesses
- `perf_modeling/state/scratchpad.py` now exposes bank-range enumeration for contiguous accesses.
- `perf_modeling/engine.py` now enforces both `mem_*` and `sp_bank_*` reservations before issue.
- `tests/test_memory_contention.py` validates:
  - DMA versus DRAM-load contention
  - DMA versus same-bank scratchpad-load contention
  - DMA versus different-bank scratchpad-load overlap
- Full verification is green with `uv run pytest tests -q` at 41 passing tests.

## Current Roadblocks

- `git push` is still expected to fail because DNS/network resolution to GitHub remains blocked in this environment.
- The worktree still contains unrelated user-thread changes in `AGENTS.md`, the deleted `main.py`, the untracked `docs/` tree, and the untracked `scripts/codex.py`.

## Current Questions

- The open toolchain clarification in `QUESTIONS.md` remains unanswered: whether the packaged repo-local GNU toolchain is acceptable, or whether a source-built `riscv-gnu-toolchain` checkout is required.

## Pending Design Decisions

- Whether to keep scratchpad contention at bank granularity only, or also model read/write port limits as separate shared reservations.
- Whether the next timing milestone should add bank-conflict penalties to latency itself, or keep contention purely issue-side for now.

## Next-Step Plan

- Commit the banked scratchpad contention slice.
- Attempt the required remote push again and record the expected failure if the network restriction persists.
- Continue into the next timing-model increment after checkpointing this shared-resource change.
